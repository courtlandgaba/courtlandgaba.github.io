$(document).ready(function() {
    $('#fullpage').fullpage({
        responsive: 984,
        scrollingSpeed: 600,
        menu: '#myMenu',
        anchors: ['home', 'about', 'work', 'resume'],

    });
});
